# clitest

Compatibility shim for [crok](https://github.com/mmastrac/crok).

`clitest` forwards to the `crok` binary and prints a deprecation warning. New
projects should use `crok` directly:

```shell
cargo install crok
```
